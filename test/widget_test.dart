import 'package:flutter_test/flutter_test.dart';

/// Full [MyApp] layout depends on ScreenUtil + device size; smoke-test networking via `api_envelope_test.dart`.
void main() {
  test('placeholder — use integration tests for full UI', () {
    expect(true, isTrue);
  });
}
